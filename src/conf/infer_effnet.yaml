# ---------- Overriding hydra default configs ----------
hydra:
  job:
    name: infer
    chdir: true
  run:
    dir: output/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

defaults:
  - _self_
  - dir: dir

img_size: 384
model_name: "tf_efficientnet_b1"
valid_batch_size: 128
best_model_bin: /kaggle/output/train/2024-08-19/06-49-06/AUROC0.5519_Loss0.0053_epoch4.bin
