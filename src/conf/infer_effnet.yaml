# ---------- Overriding hydra default configs ----------
hydra:
  job:
    name: infer
    chdir: true
  run:
    dir: output/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

defaults:
  - _self_
  - dir: dir

img_size: 384
model_name: "tf_efficientnet_b0_ns"
valid_batch_size: 64
best_model_bin: /kaggle/output/train/2024-08-18/13-31-19/AUROC0.5495_Loss0.0060_epoch4.bin
