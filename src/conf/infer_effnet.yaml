# ---------- Overriding hydra default configs ----------
hydra:
  job:
    name: infer
    chdir: true
  run:
    dir: output/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

defaults:
  - _self_
  - dir: dir

img_size: 384
model_name: "tf_efficientnet_b1"
valid_batch_size: 128
best_model_bin: /kaggle/output/train/2024-09-02/05-49-12/pAUC0.1679_Loss0.1823_epoch61.bin
