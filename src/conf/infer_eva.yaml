# ---------- Overriding hydra default configs ----------
hydra:
  job:
    name: infer
    chdir: true
  run:
    dir: output/${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

defaults:
  - _self_
  - dir: dir

img_size: 336
model_name: "eva02_small_patch14_336.mim_in22k_ft_in1k"
valid_batch_size: 64
best_model_bin: /kaggle/output/train/2024-08-17/14-58-08/AUROC0.5181_Loss0.5046_epoch25.bin

